[project]
name = "cfanres"
version = "0.1.0"
description = "A collection of tools for analyzing analysis results in ALICE"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cfanres"]
include = ["doc/** -> cfanres/data"]
